<!DOCTYPE html>
  <html>
    <head>
      <meta charset="utf-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <title>HarperDB</title>
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <link rel="stylesheet" type="text/css" media="screen" href="../css/styles.css" />
    </head>
    <body>
      <h1>HarperDB</h1>
      <h2>Some heading here</h2>
      <h3 class="table-name"><%= message %></h3>
      <% if(typeof attributes !== 'undefined' ) { %>
        <table>
          <thead>
            <tr>
              <% for (var i=0; i < attributes.length; i++) { %>
              <th><%= attributes[i] %></th>
              <%}%>
            </tr>
          </thead>
          <tbody>
            <tr>
              <% for ( var x=0; x < attributes.length; x++) {
                for ( var y=0; y < attributes.length; y++) { %>
                  <td> <%= tableData[x][y] %> </td> <%
                } %>
              </tr>
              <% } %>
          </tbody>
        </table>
      <% } %>
      <div>
        <form method="post" action="/display_insert">
          <label>Insert</label><br>
          <input type="text" name="id" placeholder="id">
          <input type="text" name="keyOne" placeholder="Attribute One">
          <input type="text" name="valOne" placeholder="Value One">
          <input type="text" name="keyTwo" placeholder="Attribute Two">
          <input type="text" name="valTwo" placeholder="Value Two">
          <input type="text" name="keyThree" placeholder="Attribute Three">
          <input type="text" name="valThree" placeholder="Value Three">
          <input type="text" name="keyFour" placeholder="Attribute Four">
          <input type="text" name="valFour" placeholder="Value Four">
          
          // Passing state
          <% if(typeof attributes !== 'undefined' ) { %> 
            <input type="hidden" name="attributes" value=<%= attributes %>
            <input type="hidden" name="tableData" value=<%= tableData %> >
          <%}%>
          <input type="hidden" name="message" value=<%= message %> >

          <input type="submit">
        </form>
      </div>
    </body>
  </html>
